/**
 * Generate a set of rules based on the specified options. The options
 * parameter can be either a single object or an array of objects to
 * specify different access rules depending on the request origin.
 *
 * @param {Object|Array} options the CORS configuration options
 * @returns {Array} An array of rule sets
 */
module.exports = function _getRulesFromOptions(options) {
  return [];
};
