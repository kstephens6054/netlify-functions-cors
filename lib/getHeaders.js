/**
 * Generate the CORS HTTP response headers based on the origin and
 * headers of the current Netlify function event.
 *
 * @param {*} event The Netlify function event object
 * @param {Array} rules The array of CORS rule sets
 * @returns {Object} The CORS HTTP headers
 */
module.exports = function _getHeaders(event, rules) {
  return {};
};
